# Dokumentasi Teknis & Panduan Deployment Sistem Pendukung Keputusan (SPK) Laptop
## Toko Super Komputer (10 Kriteria Lengkap: Metode ROC & TOPSIS Terintegrasi Supabase & Vercel)

Dokumen ini memuat spesifikasi sistem pendukung keputusan (SPK) pemilihan laptop berbasis Multi-Criteria Decision Making (MCDM) dengan **10 kriteria keputusan (C1 - C10)**, formula pembobotan **Rank Order Centroid (ROC, m=10)**, dan algoritma perankingan **TOPSIS 10 Dimensi**.

---

## 1. Informasi & Kredensial Resmi Proyek

* **Supabase Project URL:** `https://jaizwaoqbqykvyvdxjyj.supabase.co`
* **Supabase Public Key:** `sb_publishable_l9p5lowmhr4nK9-JD7B93w_tfhrTtRB`
* **GitHub Repository:** `https://github.com/bambangharmoko/pemilihanlaptop-super.git`
* **Arsitektur Front-End:** Modular Static App (`index.html`, `style.css`, `supabase.js`, `app.js`)
* **Metode SPK:** Integrasi Rank Order Centroid (ROC) + TOPSIS (Technique for Order Preference by Similarity to Ideal Solution)

---

## 2. Definisi Matriks 10 Kriteria Keputusan

| Kode | Nama Kriteria | Tipe | Satuan | Skala Nilai | Default Rank | Bobot Default ROC ($w_j$) |
| :--- | :--- | :---: | :---: | :---: | :---: | :---: |
| **C1** | Harga Beli | **Cost** | Rupiah (Rp) | Nominal Rp | 1 | **29.29%** |
| **C2** | Processor (CPU) | **Benefit** | Skor 0-100 | Benchmark Skor | 2 | **19.29%** |
| **C3** | Kapasitas RAM | **Benefit** | Gigabyte (GB) | 4 s/d 128 GB | 3 | **14.29%** |
| **C4** | Kapasitas SSD | **Benefit** | Gigabyte (GB) | 128 s/d 4096 GB | 4 | **10.96%** |
| **C5** | Kartu Grafis (GPU) | **Benefit** | Skor 0-100 | Visual/3D Skor | 5 | **8.46%** |
| **C6** | Daya Tahan Baterai | **Benefit** | Watt-Hour (Wh) | 30 s/d 120 Wh | 6 | **6.46%** |
| **C7** | Portabilitas (Berat) | **Cost** | Kilogram (Kg) | 0.8 s/d 4.5 Kg | 7 | **4.79%** |
| **C8** | Kualitas Layar | **Benefit** | Skor 0-100 | Panel & Warna | 8 | **3.36%** |
| **C9** | Masa & Layanan Garansi | **Benefit** | Skala 1–5 | Durasi & ADP | 9 | **2.11%** |
| **C10** | Kemampuan Upgrade | **Benefit** | Skala 1–5 | Ekspansi RAM/SSD | 10 | **1.00%** |
| **TOTAL** | | | | | | **100.00%** |

### Konversi Skala Kriteria Kualitatif:
- **C9 (Masa & Layanan Garansi):**
  - `1` = 1 Tahun Garansi Distributor
  - `2` = 1 Tahun Garansi Resmi
  - `3` = 2 Tahun Garansi Resmi
  - `4` = 2 Tahun Garansi Resmi + 1 Tahun ADP (*Accidental Damage Protection*)
  - `5` = 3 Tahun Garansi Resmi + ADP Lengkap
- **C10 (Kemampuan Upgrade / Upgradeability):**
  - `1` = Full On-Board / Soldered (RAM & SSD tertanam)
  - `2` = RAM On-board + 1 Slot SSD M.2 Tambahan
  - `3` = 1 Slot RAM Bebas SODIMM + 1 Slot M.2 SSD
  - `4` = Dual Slot RAM SODIMM + 1 Slot M.2 SSD
  - `5` = Dual Slot SODIMM + Dual Slot M.2 NVMe SSD

---

## 3. Skema DDL & Migrasi Database Supabase (PostgreSQL)

Jalankan perintah SQL berikut di **SQL Editor** Supabase Dashboard:

```sql
-- 1. TABEL MASTER KRITERIA (10 Kriteria)
CREATE TABLE IF NOT EXISTS public.kriteria (
    id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    kode VARCHAR(10) UNIQUE NOT NULL,
    nama VARCHAR(100) NOT NULL,
    tipe VARCHAR(10) CHECK (tipe IN ('Benefit', 'Cost')) NOT NULL,
    satuan VARCHAR(50) NOT NULL,
    keterangan TEXT,
    default_rank INT NOT NULL,
    created_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL
);

-- 2. TABEL DATA LAPTOP
CREATE TABLE IF NOT EXISTS public.laptops (
    id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    nama VARCHAR(150) NOT NULL,
    merek VARCHAR(50) NOT NULL,
    status VARCHAR(20) CHECK (status IN ('ready', 'indent')) NOT NULL DEFAULT 'ready',
    kategori_penggunaan VARCHAR(50) DEFAULT 'Umum',
    harga NUMERIC(15, 2) NOT NULL,
    cpu_score NUMERIC(6, 2) NOT NULL,
    ram_gb INT NOT NULL,
    ssd_gb INT NOT NULL,
    gpu_score NUMERIC(6, 2) NOT NULL,
    baterai_wh NUMERIC(6, 2) NOT NULL,
    berat_kg NUMERIC(4, 2) NOT NULL,
    layar_score NUMERIC(6, 2) NOT NULL,
    garansi_score NUMERIC(4, 2) NOT NULL DEFAULT 1,
    upgrade_score NUMERIC(4, 2) NOT NULL DEFAULT 1,
    spesifikasi_ringkas TEXT,
    created_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL
);

-- 3. MIGRASI KOLOM C9 & C10 JIKA TABEL SUDAH ADA
ALTER TABLE public.laptops ADD COLUMN IF NOT EXISTS garansi_score NUMERIC(4, 2) NOT NULL DEFAULT 1;
ALTER TABLE public.laptops ADD COLUMN IF NOT EXISTS upgrade_score NUMERIC(4, 2) NOT NULL DEFAULT 1;

-- 4. ROW LEVEL SECURITY POLICIES
ALTER TABLE public.kriteria ENABLE ROW LEVEL SECURITY;
ALTER TABLE public.laptops ENABLE ROW LEVEL SECURITY;

CREATE POLICY "Public Read Kriteria" ON public.kriteria FOR SELECT USING (true);
CREATE POLICY "Public Read Laptops" ON public.laptops FOR SELECT USING (true);
CREATE POLICY "Public Insert Laptops" ON public.laptops FOR INSERT WITH CHECK (true);
CREATE POLICY "Public Update Laptops" ON public.laptops FOR UPDATE USING (true);
CREATE POLICY "Public Delete Laptops" ON public.laptops FOR DELETE USING (true);

-- 5. SEED MASTER 10 KRITERIA
INSERT INTO public.kriteria (kode, nama, tipe, satuan, keterangan, default_rank) VALUES
('C1', 'Harga Beli', 'Cost', 'Rupiah', 'Budget harga pembelian toko (makin hemat makin prioritas)', 1),
('C2', 'Processor (CPU)', 'Benefit', 'Skor 0-100', 'Performa komputasi inti prosesor', 2),
('C3', 'Kapasitas RAM', 'Benefit', 'GB', 'Memori multitasking dan rendering', 3),
('C4', 'Kapasitas SSD', 'Benefit', 'GB', 'Kapasitas storage penyimpanan sistem', 4),
('C5', 'Kartu Grafis (GPU)', 'Benefit', 'Skor 0-100', 'Performa visual, gaming, dan 3D rendering', 5),
('C6', 'Daya Tahan Baterai', 'Benefit', 'Wh', 'Kapasitas baterai durasi operasional', 6),
('C7', 'Portabilitas (Berat)', 'Cost', 'Kg', 'Bobot fisik laptop (makin ringan makin baik)', 7),
('C8', 'Kualitas Layar', 'Benefit', 'Skor 0-100', 'Panel layar, warna, dan refresh rate', 8),
('C9', 'Masa & Layanan Garansi', 'Benefit', 'Skala 1-5', 'Durasi garansi resmi dan proteksi ADP (1-5)', 9),
('C10', 'Kemampuan Upgrade', 'Benefit', 'Skala 1-5', 'Fleksibilitas upgrade RAM & storage SSD (1-5)', 10)
ON CONFLICT (kode) DO UPDATE SET 
    nama = EXCLUDED.nama,
    tipe = EXCLUDED.tipe,
    satuan = EXCLUDED.satuan,
    keterangan = EXCLUDED.keterangan,
    default_rank = EXCLUDED.default_rank;
```
